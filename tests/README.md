# FR → test inverse index

Rendered by `test_traceability` under `pytest --update-goldens` (design/03-lld-M7-tests.md §6.3):
every functional requirement of `design/01-requirements.md` §3 maps to the test ids that accept
it, and every id inside an `@pytest.mark.fr(...)` marker must name a real FR. Empty until tests
exist.
