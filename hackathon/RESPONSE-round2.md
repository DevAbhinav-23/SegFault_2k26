# RESPONSE to red team round 2

*2026-09-12, rev. r3. Reviser → architect. One row per finding in
[`REDTEAM-round2.md`](REDTEAM-round2.md): 5 kills, 8 wounds, 6 nits, 1 no-flaw-found, plus the
rival statement. Every row states the disposition, what changed, and where. Section references
are to [`01-paradigm-comparison.md`](01-paradigm-comparison.md) **rev. 2026-09-12 r3**.*

**Summary: 20 of 20 findings accepted** — 17 accepted outright, 3 accepted-with-modification, 0
rejected. **This was a narrow pass by the architect's instruction**: no prose was re-audited and
nothing was restructured; only the changes below were made, plus the rescoring they force.
**The wording loop stops after r3.** Every question that could not be settled by reading is now
a named experiment with a recipe in the new **§9, "Open experiments (day-1 list)"**.

**Two scores moved, and the pick's justification was rebuilt.** M1 P5 3→4 (K4) and M4 P5 3→2
(K2); they cancel in the equal-weighted total, which is a coincidence, not a defence. §6.7's
pick keeps its *name* (Candidate A) and loses two of its three *reasons*: "M3 + M1" is now M3
alone, and "declared spatial intent" is no longer novel. What holds it up in r3 is the
**pre-codegen legality check**, which is also the rival's proposal — see the last row.

---

## 1. Kills

| # | Finding | Disposition | What changed, and where |
|---|---|---|---|
| **K1** | The put-before-get halo protocol deadlocks at AIR's default depth 1 | **Accepted as UNRESOLVED and EMPIRICAL** (architect's adjudication: claim neither safety nor deadlock) | **§1.1 fact 1 rewritten** to quote *both* halves — the stall rule (*"A `put` issued when the channel already holds `depth` unread transfers stalls…"*, under which the first put into an empty depth-1 channel does not stall) **and** the minimal-deadlock example's comment (*"// put blocks: channel is at capacity, waiting for a get to free space"*, under which it does) — **plus the asynchronous sentence verbatim**, and states plainly that the page supports both readings and reconciles neither. **§3.2's protocol is restated**: each PE issues its two puts in **async** form and its two gets with **no token dependency** on them, because they touch different buffers and `air-enforce-channel-fifo-order`'s own description says *"air-dependency only orders channel ops that share a buffer"* (A61) — so under the async reading both neighbours' gets consume each other's puts, and under the rendezvous reading it deadlocks. Marked **`[UNVERIFIED — Experiment E1]`**. **(c)** every W2 (d)/(e) cell that said "not reachable" / "compile time" now reads **"not reachable if E1 passes"** (W2·P1 (e), W2·P5 (e)). **(d)** M4 re-derived for **P1** (held at 4, reason restated, with "if E1 fails this cell is 3" stated in the cell) and **P5** (3→2, jointly with K2). **Hardware note added and sourced** (A60): mlir-aie's own design-pattern doc shows MM2S in the source tile's `AIE.mem` and S2MM in the destination's, so outbound and inbound are separate DMA channels — which removes an *engine-level* deadlock mechanism but not the *channel-slot* question. **AMD AM020 was not fetched and is not cited.** **E1's recipe is §9.** |
| **K2** | W2·P5's *"sequential oracle: yes, unconditionally"* is false | **Accepted** | **W2·P5 (c) rewritten.** `spatial-dsl/04` §4 fixes the fallback as *"loop over all `(pi,pj)`"* — grid loop outermost — so with `sp.exchange` inside a time loop PE 0 runs all `T` steps before PE 1 runs any; the flat-array escape fails because the halo dependence is bidirectional *within* a timestep and because `u, v = v, u` swaps named buffers. **The oracle exists only with a lockstep (timestep-outermost) interpreter over PEs — i.e. the interpreter §3.0 charges against P2.** Stated plainly as **a genuine P1-vs-P5 discriminator on W2**: P1's nest has `t` outermost and the schedule is strippable, so P1's W2 oracle *is* unconditional. **M4's P5 cell rescored 3 → 2** from the anchor's oracle clause (none of the four 5-clauses now holds outright; not 1, because the oracle survives unconditionally on W1 and W3). §3.0's P5 glossary now says `sp.exchange`'s fallback is **not** a no-op. **Experiment E2** added (§9), with the expected mismatch under `p`-outermost and match under `t`-outermost, and the cell explicitly says E2 can move it back to 3. §6.5's D1 row now gates W2 on E2 and budgets the interpreter. |
| **K3** | By §1.4's own test, the fallback the pick degenerates to is *"not worth a week"* | **Accepted; architect's decision implemented — the fallback is redefined** | **Candidate B ("nothing declared") is dropped as a candidate** and the reason is recorded once in §6.2, including that it is Triton-XDNA's shape and that its only delta would be an oracle against Triton's interpreter mode. **The fallback is now B′ = the P5 core with declared intent** — `sp.acc(stationary=True)`, `sp.load(bcast=`/`flow=`), `sp.exchange` — which claims **both** §1.4 properties. Made consistent in **§6.2** (candidate definition and the A-vs-B′ difference, now "ignorable surface + legality check", not "declared intent"), **§6.3** (the B column is retitled **B′** and all four rows rewritten), **§6.5** (plan header, D1, D6, fit check), **§6.6** (two rows) and **§6.7** ("why this and not B′ outright"). §1.4 gained a short paragraph recording that the pure-derived candidate was considered and why it failed the test. |
| **K4** | M1 inverts between P1 and P5 against the document's own counting rule | **Accepted with modification** (architect: restore `s.stream`, apply the invention penalty uniformly or drop it, then re-derive) | **`s.stream(x, pattern="broadcast"/"forward"/"cascade", along=ax)` restored to P1's §3.0 glossary**, marked explicitly as **not an invention** — `spatial-dsl/01` §3's vocabulary table has `stream(buf: pattern)` with *"`broadcast`, `forward` (systolic), `cascade` (reduce)"*. **The invention penalty is dropped as a discriminator**, stated in §3's preamble, with ✚ marks now on **both** columns' halo primitives so a reader can re-apply it symmetrically. **Re-derived from the anchor: P1 = 4** (five of six statable; the absence is non-neighbour communication) and **P5 = 3 → 4** (five of six; the absence is wavefront skew, and P5 *can* state non-neighbour communication, which P1 cannot). **W1·P1's sketch gained two `s.stream` lines** (18 → 20 lines, and P1's §5 line total 48 → 50) and its (b) table's multicast row now reads "declared **and** derived, the declaration checked against the derivation". **Reported honestly: this flips §6.6's deciding row from "M3 + M1" to "M3 alone", halving its margin, and §6.7 is re-derived rather than protected** — see the summary above and the K5 row. |
| **K5** | §6.3's AIEHalide row is built from 5 lines of a 304-line file whose remainder contradicts it | **Accepted** | `PACT/56.txt` **read in full**. **§6.3's row, §7.2's paragraph and A57 all rewritten.** Corrections: the paper is **ACCEPTED** (L300, *"We are pleased to inform you that your paper has been accepted."*) — *"in-submission"* and *"not independently verifiable"* withdrawn; the rebuttal author is **Abnikant Singh, IIIT** (L249), a different person, so **the file establishes no link to the user** and the *"our group"* attribution is cited to its real source, the user's proposal (`proposals/spatial_dsl_project_proposal.md` L9, *"Our recent work demonstrated an end-to-end **Halide-to-AMD-NPU compiler**, published at **PACT 2026**"*), **labelled a user-supplied fact**; AIEHalide keeps Halide's standard directives and adds **three optional expert directives** (L256, quoted verbatim: `aie_dataflow`, `aie_fuse_with`, `aie_kernel`); **derives halos** via bounds inference (quoted); **synthesises dataflow into MLIR-AIE, bypassing AIR** (quoted); measured **53%/62% of peak** on XDNA/XDNA2 for int8 GEMM against StB's 66%/93% (L51–53). **Candidate A's delta re-derived as four things**: (i) AIR target vs MLIR-AIE; (ii) a plain loop nest vs Halide `Func` pipelines; (iii) a named space-time vocabulary — **checked**: `src/Func.h` (2 972 lines) has **zero** hits for skew/wavefront/stationar/diagonal and the full directive list has no such directive (A64, with the URL); (iv) **a pre-codegen legality check**, absent there by AIEHalide's own rebuttal (*"appear as a compile-time routing failure"*, *"an explicit feedback loop is future work"*). **"Declared spatial intent" is stated outright as no longer claimable as novel** (§1.4, §6.3, §6.7, executive summary). **W2·P1 (e)'s "strongest single argument for P1"** now says derived halos are not a novelty argument because AIEHalide already derives them, and is restricted to a claim *inside this comparison*. |

## 2. Wounds

| # | Finding | Disposition | What changed, and where |
|---|---|---|---|
| **W1** | M5 = 5 for P5 is unreachable from its own anchor | **Accepted** | Resolved by the uniform rebuild (W2 row). P5's rebuilt counts are **2 / 3 / 3**, so the ≤3 clause is met and the 5 is reachable; the cell now reports those counts instead of r2's "3 on W1, 4 on W2/W3", and carries §4's ±1 caveat explicitly (*"a reader who charges S12-equivalent work to P5 reads this as 4"*). |
| **W2** | The F1–F3 floor is still not uniform — in both directions | **Accepted** | **§3.0 gained F4** (token-edge emission, previously charged to P3/P4/P5 only) and **one rebuilt step table**: a fixed 12-step vocabulary (S1–S12), F1–F4 excluded for every column, **channel materialisation (S5) charged to every column with channels including P5**, and a per-column-per-workload grid with a max row. New counts (max): **P1 5, P2 4, P3 3, P4 4, P5 3**. All fifteen (d) sections rewritten to cite S-numbers and to say which r2 steps were floor. **Every M5 cell re-derived from the anchor using max-over-workloads**; the scores happen not to move (2/2/4/3/5), and the cells say why each does or does not meet each 5-clause. **§4 gained the ±1 noise note** the architect asked for, in the M5/M7 notes. |
| **W3** | Triton-XDNA ships a schedule surface | **Accepted** | **§6.3's Triton-XDNA row and §7.2 rewritten.** Verified independently for r3: 53 transform paths in the repo tree; `examples/matmul_bf16_m64_n64_k64/transform_aie2.mlir` fetched and read (270 lines), header *"Auto-generated by matmul_transform.py — do not edit manually."* and phase headers quoted verbatim including *"PHASE 2: PROMOTE OUTPUT TO L2"* and *"PHASE 5: TILE FOR MULTI-CORE PARALLELISM"* / *"Tile [16, 16, 0] for herd distribution."*; `AIR_TRANSFORM_TILING_SCRIPT` confirmed in the README's env table as *"Path to the MLIR transform dialect tiling script"* (A63). **Memory-space promotion and herd width are stated as user-settable today.** The delta is restated as **intent bound to the program's own loops and tensors rather than to a detached auto-generated script, plus the four constructs**, whose zero occurrences across 47 example kernels are **attributed to the red team's grep as their evidence** and labelled as not independently re-run. **The "flip demo" answer is addressed head-on** in §6.3 and §6.5: the reply to *"swap the transform script"* is that the script moves tiles and memory spaces and has nothing that names stationarity, so it cannot move `stationary` from `C` to `B` — and §6.5 now says the **rejection** demo, not the flip, is the stronger 60 seconds. |
| **W4** | SpaDA — nearest neighbour to the document's own novelty claim — is still unread | **Accepted** | arXiv:2511.09447v2 fetched and the **PDF extracted locally**; abstract, §I, the language section incl. Listing 1, and the GT4Py lowering section read. **A full paragraph added after §7.3's table**, plus a new row **A65** and a rewritten §7.3 row. **Exactly what was read and not read is labelled** in both. Content: it declares *"precise control over data placement, dataflow patterns, and asynchronous operations"*, with `place`/`compute`/`dataflow` blocks, `relative_stream(x, y)`, cardinal-direction multicast, and async/await — so **§1.4's property (b) is a category, not an invention**; its **halo is derived** one level up in a GT4Py stencil IR, which is a point *for* P1's derived halo and against both columns' invented `exchange`; it has **no stationarity and no skew directive and no legality check**; and it **targets Cerebras CSL, not AIE**. §7.4 gained the query r2 failed to run. |
| **W5** | §6.1's exclusion arithmetic contradicts itself | **Accepted** | §6.1's sentence rewritten: **P2's 3–5 pw fits entirely inside N = 4's 5.6 pw and at its optimistic end inside N = 3's 4.2 pw — i.e. it is cheaper than P1-full, which is carried.** P2 is excluded **not on cost** but for **M3 = 1** and the **two whole-program analyses** in its W1 lowering. P4 alone keeps the "out at any plausible *N*" statement. The reason is given explicitly so a jury asking "why not actors?" gets the real answer. |
| **W6** | `sp.flow` was redefined, and its stated fallback does not work | **Accepted** | **§3.0's P5 glossary splits the three constructs**: `spatial-dsl/04`'s own `sp.flow(slice, dir=)` (a *delivery-mode override on an operand load*, fallback *"slice"*), the invented **`sp.flow(x, dir=)`** put, and the invented **`sp.flow_in(dir=)`** get. **Both kernel-body forms are marked ✚**, and a paragraph records that r2's preamble marked one while its table marked two. **W3·P5's sketch now uses `sp.flow_in`**, and its (e) rows follow. **W3·P5 (c) rewritten**: with `p` outermost the link must **buffer `MQ` values**, the visit order is **column-block-major** not row-major, and the **values are still correct** because the chain is acyclic — with the cost named (`O(MQ·PJ)` of buffers the "free oracle" story does not advertise). |
| **W7** | Every *"AIR catches it at compile time"* rests on a checker the primary source never names | **Accepted, and the search was run** | **§1.1 fact 2 rewritten** to say the compute model asserts compile-time enforcement and **names no pass**, then to report the r3 search: `AIRDialect.cpp` (4 069 lines) has **zero** hits for "balanc"/"acycl"; `air::ChannelOp::verify()` and `air::ChannelPutOp::verify()` were **read** and check only channel-type allow-lists, NumPy broadcast compatibility, `refeed_count`, `packet_ids`, sizes/strides rank, the `npu_mmio`/`gpu_symmetric_heap` scope rules and the not-an-`scf.for`-IV bundle-index rule; `Transform/Passes.td` has no balance or acyclicity pass. **`air-enforce-channel-fifo-order` is described** as the architect asked — *"Serialize same-channel async ops to preserve FIFO order"*, an ordering repair whose description supplies the load-bearing sentence for K1's protocol. **`air-verify-hierarchy-locality` was also found** and is used honestly: it detects **races**, not balance, and its applicability to W1·P5's shape is folded into E3. **Marked `[UNVERIFIED which pass — Experiment E3]`** and **all ten remaining "AIR catches it / re-checks / would reject" cells softened**, in W1·P1 (e), W1·P2 (e), W1·P3 (e), W1·P5 (e), W2·P1 (e), W2·P2 (e), W2·P3 (e) and its narrative, W3·P1 (e), W3·P2 (e), W3·P5 (e), and in the M4 justifications for P1, P2, P3 and P5. New rows A59, A61. |
| **W8** | The stated fix for K2 violates a balance sub-bullet the document never quotes | **Accepted; it changed the K2 fix** | **All four balance bullets are now quoted verbatim** in §1.1 fact 2 and in A5. **§3.0's "peel the loop" fix is deleted**, with the reason stated: `T−1` puts against `T` gets in one loop body violates *"balance must hold **per iteration**"*, suppressing the last put fails the same bullet, and guarding it with an `if` fails the per-branch bullet. **The correct statement is in its place**: the put-first protocol needs **no prologue and no peel** — 2 puts + 2 gets per PE per iteration, `T` iterations, per-iteration balanced — and the `t = 0` ghost is the neighbour's initial boundary row sent in iteration 0. Repeated in §3.2's preamble. **W3·P5's `if p < PJ-1: sp.flow(...)` was checked against the conditional-branch bullet and argued legal**, in W3·P5 (d): balance is stated **per channel index** and the bundle is indexed per link, so each index sees one put and one get on the taken branch and zero-against-zero on the not-taken one — **the guards delete whole indices, they never leave one unbalanced** — with indices 0 and `PJ` filled by step S7's generated source/drain. Separately verified and noted: `air::ChannelPutOp::verify()` rejects an `scf.for`-IV bundle index, and `p` here is a **herd** coordinate, so the indexing form is legal. |

## 3. Nits

| # | Finding | Disposition | What changed, and where |
|---|---|---|---|
| **N1** | §0's grep membership is wrong | **Accepted** | §0 re-verified for r3: `grep -ril akka` now returns **four** files, all in `hackathon/` — this one, `REDTEAM-round1.md`, `REDTEAM-round2.md`, `RESPONSE-round1.md`. `HANDOFF.md` has **zero** hits. The text now names them, says the count was right and the membership wrong, and notes that **the set grows by one each round, so only the "outside `hackathon/`" half of the claim is stable**. |
| **N2** | *"the shortest W2 sketch"* is false against the document's own counts | **Accepted** | W2·P1's caption drops the comparative and records the correction: W2·P5 = 13 and W2·P3 = 15, both counted by this document. (W3·P1's *"shortest P1 sketch"* caption was re-checked and is correct: 14 vs 16 and 20.) |
| **N3** | *"Nine cells moved from r1"* — it was eight | **Accepted** | §5's header now says **eight** moved from r1, cites `RESPONSE-round1.md` and the column deltas, and adds the **two r3 moves** (M1 P5 3→4, M4 P5 3→2) with a note that they cancel in the total by coincidence, not by design. |
| **N4** | A51's Triton-XDNA quotes are markup-stripped | **Accepted** | A51 now states that the quotes are **markup-stripped, not character-exact** — inline markdown links inside the first, bold emphasis inside the performance claim — and that wording and word order are exact. |
| **N5** | §1.1 fact 4 presents two disagreeing scope rules as one | **Accepted** | §1.1 fact 4 now separates them: the **module docstring** says an L3 endpoint *"has to sit inside an `air.segment`"*, while the **implemented check**, re-read for r3, requires an enclosing `air.launch` for any L3 endpoint and a segment **only inside a herd body**. Stated: the docstring is stricter than the code it documents, nothing below depends on the difference, and **no claim may cite the docstring as the rule**. |
| **N6** | mlir-air ships a legacy CPU backend | **Accepted; the answer is proposed** | §1.4 item 1 rewritten. `python/air/backend/cpu_backend.py` verified and quoted: `AirCpuBackend`, *"This currently uses the torch-mlir linalg-on-tensors RefBackend for JIT execution."*, `DEFAULT_PIPELINE` = `air-to-async, canonicalize, cse`, an `LLVMJITBackend`, and **no reference to it from any of the twelve `python/air/api/` files**. The honest claim is stated — *"an `air.api` **program text** does not run as its own specification, though an AIR **module** can be JIT-run on CPU by a different, unconnected backend"* — together with the concession that **a jury may not grant the distinction**, and **the answer to give**: a CPU JIT of the lowered IR tests the compiler's output, not the user's source, so it cannot catch a wrong slice expression because it executes the slices the compiler already derived. New row A62. |

## 4. No-flaw-found

| # | Finding | Disposition | What changed, and where |
|---|---|---|---|
| **B14** | §1.4's *"No sequential oracle"* claim is correct and should be upgraded | **Accepted** | **B14 marked RESOLVED in this document's favour and promoted to Appendix A as row A59**, with the red team's evidence re-verified line-by-line for r3: `_compile.py` L8, *"The body is not executed when ``@launch.body`` runs -- it is recorded, and replayed later"*; `_trace.py` L8, *"Nothing here emits IR until a herd body is registered."*; twelve files, no `_interpret*`/`_sim*`/`_eval*`. §1.4 item 1 now states it as verified rather than inferred. The only surviving caveat is N6's, and it is a different claim. |

## 5. The rival statement

| Finding | Disposition | What changed, and where |
|---|---|---|
| **Build the verifier, demo a rejection** | **Accepted into the document as a live alternative, with the architect's position stated and the choice left to the user** | **New §6.8, "The rival: ship the missing checker."** The rival's case is stated at full strength first — take Triton-XDNA and AIEHalide as given, neither tells you before codegen that a mapping is illegal, the AIR model states balance and acyclicity with no pass named (**and r3 looked and could not find one**), so ship the verifier and make the demo a rejection — with all four of its arguments carried, including that it beats IRONSmith on content rather than losing on pixels. **Then the architect's position**: Candidate A's D3 checker **is** this component (acyclicity + per-iteration balance + `(σ,π)` legality), so A subsumes the rival's core while also delivering a surface — and the surface is what makes the *intent* question askable at all, since nothing in raw AIR IR says `stationary`. **And the named case where the rival wins, stated as an instruction: if *N* = 1 and E1 fails, ship the rival.** Reinforced in the executive summary, in §6.6 (two new rows), in §6.7's closing conditions, and in §9's E1. |

---

## 6. What changed that the red team did **not** ask for

Nothing structural. Three additions follow from the architect's brief rather than from a
finding, and round-3-plus readers should know they are new surface area:

1. **§9, "Open experiments (day-1 list)"** — E1 (depth-1 put-first exchange), E2 (W2·P5
   fallback order), E3 (does `air-opt` reject unbalanced/cyclic programs, and which pass), E4
   (do the ping-pong passes fire on an `air.api`-emitted herd loop — B15), E5 (end-to-end
   Versal — B13, optional). Each has a recipe, an expected outcome under each hypothesis, and
   the specific text in this document that changes if it fails.
2. **A "D0" half-day in §6.5** ahead of D1, to run E1 and E2 before the plan commits.
3. **Three new Appendix B items and one resolution**: B18 (`[UNVERIFIED — E2]`, the W2·P5
   oracle argument was reasoned, not run), B19 (the r3 rescores are judgements on re-read
   evidence), an extended B12 (the contradiction, and that the checker is *unlocated*, not
   merely un-run), and **B14 resolved**.

**Six new Appendix A rows, all fetched or read on 2026-09-12**: A59 (`air.api` is a tracing
builder; no balance/acyclicity check in the dialect), A60 (MM2S/S2MM from mlir-aie's design
patterns — **not** AM020, which was not fetched), A61 (`air-enforce-channel-fifo-order`,
`air-verify-hierarchy-locality`, `air-dependency`), A62 (`cpu_backend.py`), A63 (Triton-XDNA
transform scripts), A64 (Halide `Func.h` has no skew/wavefront/stationarity directive), A65
(SpaDA). A4, A5, A51, A57 and A58 were corrected or extended.

---

*End of response. **rev. 2026-09-12 r3.** The wording loop stops here by the architect's
decision. Nothing in this document or its companion has been implemented or measured; §9 is
the list of things that must be.*
